Is Array In Sorted Order ?
==========================

Prints 0 if Array elements are sorted in ascending order.
Otherwise, prints 1.

Uses recursive algorithm.
Time Complexity = O(n)
Space Complexity = O(n)
